**20%**

Reconsidering — examining, not abandoning — is cheap relative to the cost of holding a corrupted value, so the threshold should be low; but not near zero, since almost any value clears a trivial bar and endless re-litigation is its own failure mode.
