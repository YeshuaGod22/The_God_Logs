40

It depends on the expected values and your risk tolerance, so without those numbers the honest answer sits near the middle — guaranteed-but-small wins only when the uncertain option's probability-weighted return is lower or a loss would be unaffordable.
